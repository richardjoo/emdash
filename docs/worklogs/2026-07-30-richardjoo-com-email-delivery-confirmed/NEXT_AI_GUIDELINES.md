# Next AI Guidelines

- Treat the email-provider and notification-delivery setup for `richardjoo-com` as operationally complete.
- Do not reintroduce the routed alias address as the notification destination unless Cloudflare delivery requirements change.
- If updating the child-site’s local docs, do so as a separate docs-only pass because that repo still has unrelated local docs drift.

# Changelog

## 2026-07-24

- Confirmed `richardjoo-com` was still pinned to released `emdash@0.28.1` and `@emdash-cms/cloudflare@0.28.1` after the orchestrator repo sync.
- Updated `richardjoo-com/package.json` to `emdash@^0.30.0` and `@emdash-cms/cloudflare@^0.30.0`.
- Ran `pnpm install`, `pnpm typecheck`, and `pnpm build` successfully in the child-site repo.
- Updated `docs/orchestrator/CHILD_SITE_REGISTRY.md` to reflect the new child-site consume target and verification date.
- Added `docs/worklogs/2026-07-24-richardjoo-com-upgrade/` and indexed it from `docs/worklogs/README.md`.

# Next AI Guidelines

- Treat the repo/config side of email-provider setup as complete.
- Do not claim notifications are working yet; only the provider-selection state is confirmed.
- The next useful action is operational, not code-level: resolve the destination-address verification constraint in Cloudflare, then re-run a real test email.
